template
    .ui.grid.palette(ref="element")
        .item(
            repeat.for="action of actions"
            data-tooltip="${action.name}"
            data-position="bottom right"
            data-inverted=""
            data-variation="basic"
        )
            img(src="${action.icon}" width="16px" height="16px")
