template
    h2 Select Deployment Credentials
    .ui.fluid.search.selection.dropdown(ref="dropdown")
        input(type="hidden")
        .default.text Credential
        .menu
            .item(
                repeat.for="credential of credentials"
                data-value="${credential.id}"
            ) ${credential.name}
    .ui.button.blue(click.delegate="deploy()")
