template
    .ui.middle.aligned.center.aligned.grid
        .column
            h2.ui.teal.image.header
                img.image(src='assets/images/logo.png')
                .content
                    | Sign up for an account
            form.ui.large.form
                .ui.stacked.segment
                    .field
                        .ui.left.icon.input
                            i.user.icon
                            input(
                                type='text',
                                name='username',
                                placeholder='Username',
                                value.bind='user.username'
                            )
                    .field
                        .ui.left.icon.input
                            i.user.icon
                            input(
                                type='text',
                                name='email',
                                placeholder='E-mail address'
                                value.bind='user.emailAddress'
                            )
                    .field
                        .ui.left.icon.input
                            i.lock.icon
                            input(
                                type='password',
                                name='password',
                                placeholder='Password',
                                value.bind='user.password'
                            )
                    .ui.fluid.large.teal.submit.button(click.delegate="submit()") Login
                .ui.error.message
            .ui.message
                | Already have an account?
                a(href='#/login') login