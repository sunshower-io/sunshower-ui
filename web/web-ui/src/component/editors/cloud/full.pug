template
    require(from="./basic")
    require(from="./credentials")
    .overlay-sidenav
        .overlay-header
            .ui.small.image.overlay-header-image
                img(src="${cloud.icon}")
            span.overlay-header-label ${cloud.name}
        .overlay-segment
            .segment-header Details
            .segment-body
                .item
                    | Created
                    | Date
                .item
                    | Updated
                    | Date
                .item
                    | Blocks
                    | Count
        .overlay-segment
            .segment-header Activity
            .segment-body
                //TODO
    .overlay-body
        .ui.secondary.pointing.menu.overlay-body-tab-menu
            a.item(
                class="${component.active ? 'active' : ''}"
                repeat.for="component of components",
                click.delegate="setActive(component)"
            ) ${component.name}
        .overlay-form
            compose(
                model.bind="cloud"
                view-model="./${activeComponent.path || 'basic'}"
            )
            //credentials(node.bind="cloud")
            //basic-virtual-cloud-editor

