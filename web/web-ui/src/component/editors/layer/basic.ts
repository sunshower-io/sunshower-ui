import {ElementEditor} from "canvas/element/element";
import {CompositeElement} from "component/model/layer";

export class BasicLayerEditor implements ElementEditor<CompositeElement> {

    open(e: CompositeElement) {
    }

}