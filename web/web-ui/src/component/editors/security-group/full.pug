template
    require(from="./basic")
    .overlay-sidenav
        .overlay-header
            .ui.small.image.overlay-header-image
                img(src="${secGroup.icon}")
            span.overlay-header-label ${secGroup.name}
        .overlay-segment
            .segment-header Details
            .segment-body
                .item
                    | Created
                    | Date
                .item
                    | Updated
                    | Date
                .item
                    | Blocks
                    | Count
        .overlay-segment
            .segment-header Activity
            .segment-body
                //TODO
    .overlay-body
        .overlay-form
            basic-security-group-editor