template
    //TODO hide sidenav and top navigator if not in the overlay
    .overlay-sidenav
        .overlay-header
            .ui.small.image.overlay-header-image
                img(src="${applicationDeployment.icon}")
            span.overlay-header-label ${applicationDeployment.applicationName}
            span.overlay-header-sublabel Version Goes Here
        .overlay-segment
            .segment-header Details
            .segment-body
                .item
                    | Created
                    | Date
                .item
                    | Updated
                    | Date
                .item
                    | Blocks
                    | Count
        .overlay-segment
            .segment-header Activity
            .segment-body
                //TODO
    .overlay-body
        .ui.secondary.pointing.menu.overlay-body-tab-menu
            .item.active Summary
            .item Stuff
            .item Stuff2
        .overlay-form
            //h2 Form Header
            .field.overlay-field
                .overlay-label
                    label This is a label!
                .ui.input.overlay-input
                    input(type="text")
                .overlay-help
                    i.icon.help.circle.outline.large

