template
    .ui.vertical.footer.segment.hasli--footer
        .ui.container
            .ui.right.floated.horizontal.list
                .disabled.item © Hasli, Inc.
                a.item(click.delegate="incompleteFeature.notify($event)") Terms
                a.item(click.delegate="incompleteFeature.notify($event)") Privacy Policy
                i.item.mail.outline.icon(
                    class="${errors && errors.length > 0 ? 'red clickable' : 'disabled'}"
                    click.delegate="clickErrors()"
                )
                    | ${errors && errors.length > 0 ? errors.length : ''}
            .ui.horizontal.list
                a.item(click.delegate="incompleteFeature.notify($event)") Docs
                a.item(click.delegate="incompleteFeature.notify($event)") Blogs
                a.item(click.delegate="incompleteFeature.notify($event)") Help & Support