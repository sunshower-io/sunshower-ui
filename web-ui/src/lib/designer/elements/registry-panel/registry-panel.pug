template
    .designer-panel-body
        .designer-form
            label Registries
            select.browser-default
                option(value='', disabled='', selected='') Choose a registry
                option(repeat.for="registry of registries" value="${registry.id}") ${registry.name}
            a.create-link(title="Incomplete Feature")
                i.material-icons.mdi.mdi-plus-circle-outline
                | Add Registry
        .designer-cards
            .designer-card(repeat.for="container of containers")