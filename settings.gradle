include(
        'aws',
        'api',
        'csp',
        'core',
        'common',
        'security',
        'service',
        'web',
        'ui'

)

def mappings = [
        'api'    : [
                'common',
                'core',
                'csp',
                'aws',
                'security'
        ],
        'service': ['security'],
        'web'    : ['ui', 'service']
]


mappings.each {
    rootDir,
    projectList ->
        projectList.each { name ->
            project(":$name").projectDir = new File(settingsDir, "$rootDir/$name")
        }
}
