include(
        'aws',
        'hal',
        'audit-api',
        'common-api',
        'core-api',
        'security-api',
        'vault-api',
        'integration-api',
        'integration-spring',
        'persist-api',
        'persist-core',
        'persist-jpa',
        'persist-hibernate',
        'service-security',
        'st-common',
        'st-security',
        'st-web',
        'test-common',
        'test-persist',
        'test-serialization',
        'test-service',
        'hasli.io',
        'web-service',
        'web-site',
        'web-ui'
)

def mappings = [
        'api' : [
                'audit-api',
                'common-api',
                'core-api',
                'security-api',
                'vault-api'
        ],
        'integration' : [
                'integration-api',
                'integration-spring'
        ],
        'persist' : [
                'persist-api',
                'persist-core',
                'persist-jpa',
                'persist-hibernate'
        ],
        'service' : [
                'service-security'
        ],
        'system-tests' : [
                'st-common',
                'st-security',
                'st-web'
        ],
        'test' : [
                'test-common',
                'test-persist',
                'test-serialization',
                'test-service'
        ],
        'web' : [
                'hasli.io',
                'web-service',
                'web-site',
                'web-ui'
        ]
]


mappings.each {
    rootDir,
    projectList ->
        projectList.each { name ->
            project(":$name").projectDir = new File(settingsDir, "$rootDir/$name")
        }
}
