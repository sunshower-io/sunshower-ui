include(
        'aws',
        'hal',
        'service',
        'web:hasli.io',
        'web:service',
        'web:site',
        'web:ui',
        'api:core-api',
        'api:common-api',
        'api:security-api',
        'service:security',
        'integration:api',
        'integration:spring',
        'persist',
        'persist:api',
        'persist:persist-core',
        'persist:jpa',
        'persist:hibernate',
        'system-tests',
        'system-tests:common',
        'system-tests:security',
        'system-tests:web',

)

def mappings = [
//        'api'    : [
//                'common',
//                'core',
//                'csp',
//                'aws',
//                'security'
//        ],
//        'service': ['security'],
//        'web'    : ['ui', 'service', 'site', 'webapp'],
//        'integration' : ['api', 'spring']
]


mappings.each {
    rootDir,
    projectList ->
        projectList.each { name ->
            project(":$name").projectDir = new File(settingsDir, "$rootDir/$name")
        }
}
